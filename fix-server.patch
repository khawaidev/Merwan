--- server/routes/order.ts
+++ server/routes/order.ts
@@ -16,7 +16,7 @@
     const internalOrderId = uuidv4();
     
     // Convert to paise
-    const razorpayOrder = await razorpayService.createOrder(amount * 100, internalOrderId);
+    const razorpayOrder = await razorpayService.createOrder(Math.round(amount * 100), internalOrderId);
     
     ordersDb.set(internalOrderId, {
